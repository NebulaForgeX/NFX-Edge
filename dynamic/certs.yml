# TLS 证书配置
# 使用动态配置文件定义证书，避免 Docker labels 语法问题

tls:
  certificates:
    # Traefik Dashboard
    - certFile: /certs/websites/lucaslyu/cert.crt
      keyFile: /certs/websites/lucaslyu/key.key
      stores:
        - default

    # www.sjgztea.com
    - certFile: /certs/websites/sjgztea/cert.crt
      keyFile: /certs/websites/sjgztea/key.key
      stores:
        - default

    # admin.sjgztea.com
    - certFile: /certs/websites/sjgztea/cert.crt
      keyFile: /certs/websites/sjgztea/key.key
      stores:
        - default

    # www.pqttec.com
    - certFile: /certs/websites/www_pqttec/cert.crt
      keyFile: /certs/websites/www_pqttec/key.key
      stores:
        - default

    # www.lucaslyu.com
    - certFile: /certs/websites/lucaslyu/cert.crt
      keyFile: /certs/websites/lucaslyu/key.key
      stores:
        - default

