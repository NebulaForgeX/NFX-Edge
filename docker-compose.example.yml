# Docker Compose 服务定义示例
# 使用方法：复制此文件为 docker-compose.yml，然后根据实际情况修改配置
services:
  # === 🔰 Traefik Reverse Proxy ===
  # Traefik 反向代理服务
  reverse-proxy:
    image: traefik:latest
    container_name: NFX-Edge-Reverse-Proxy
    restart: always

    # Traefik 命令行参数
    command:
      # 启用 Dashboard + Docker Provider
      - --api.dashboard=true
      - --providers.docker
      - --providers.docker.exposedByDefault=false

      # 文件配置（动态规则目录）
      - --providers.file.directory=/etc/traefik/dynamic
      - --providers.file.watch=true

      # EntryPoints
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443

    # 端口映射
    ports:
      - "80:80"
      - "443:443"

    # 数据卷挂载
    volumes:
      # 挂载 Docker socket
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # ✅ 挂载每个网站的证书目录
      # 挂载网站证书目录，每个网站使用独立文件夹的证书
      # 不再使用 acme.json，改为使用文件证书（cert.crt 和 key.key）
      - ${CERTS_DIR}:/certs/websites:ro
      # Traefik 主配置文件
      - ${TRAEFIK_CONFIG_FILE}:/etc/traefik/traefik.yml:ro
      # 动态规则文件目录
      - ${TRAEFIK_DYNAMIC_DIR}:/etc/traefik/dynamic:ro

    # Traefik 路由标签
    labels:
      - traefik.enable=true
      # Traefik Dashboard（根据实际域名修改 traefik.example.com）
      # 证书配置在 dynamic/certs.yml 中统一管理
      - traefik.http.routers.web-dashboard.rule=Host(`traefik.example.com`) && (PathPrefix(`/dashboard`) || PathPrefix(`/api`))
      - traefik.http.routers.web-dashboard.entrypoints=websecure
      - traefik.http.routers.web-dashboard.tls=true
      - traefik.http.routers.web-dashboard.service=api@internal
      # BasicAuth 密码哈希（需要根据实际密码生成，使用 htpasswd 工具）
      - traefik.http.middlewares.dashboard-auth.basicauth.users=admin:$$2y$$05$$KymEIDjaRPneTxHf1RCzruglmhsOdj2KQJhG6qKAUbtFkG0kCVJ/e$$
      - traefik.http.routers.web-dashboard.middlewares=dashboard-auth

    # 网络配置
    networks:
      - nfx-edge

  # === 🌿 示例网站服务 ===
  # 网站服务模板（复制此服务并修改配置即可添加新网站）
  www_example:
    image: nginx:alpine
    # 根据实际网站修改容器名称
    container_name: NFX-Edge-WWW-EXAMPLE
    restart: always

    # 数据卷挂载
    volumes:
      # 修改为实际的网站文件目录
      - ./www.example.com:/usr/share/nginx/html:ro
      # Nginx 配置文件
      - ${NGINX_CONFIG_FILE}:/etc/nginx/conf.d/default.conf:ro

    # Traefik 路由标签
    labels:
      - traefik.enable=true
      # 修改为实际的域名
      # 证书配置在 dynamic/certs.yml 中统一管理
      - traefik.http.routers.example.rule=Host(`example.com`) || Host(`www.example.com`)
      - traefik.http.routers.example.entrypoints=websecure
      - traefik.http.routers.example.tls=true

    # 网络配置
    networks:
      - nfx-edge

    # 服务依赖
    depends_on:
      - reverse-proxy

  # === ⚙️ 示例管理后台服务 ===
  # 管理后台服务模板（复制此服务并修改配置即可添加新管理后台）
  admin_example:
    image: nginx:alpine
    # 根据实际管理后台修改容器名称
    container_name: NFX-Edge-Admin-EXAMPLE
    restart: always

    # 数据卷挂载
    volumes:
      # 修改为实际的管理后台文件目录
      - ./admin.example.com:/usr/share/nginx/html:ro
      # Nginx 配置文件
      - ${NGINX_CONFIG_FILE}:/etc/nginx/conf.d/default.conf:ro

    # Traefik 路由标签
    labels:
      - traefik.enable=true
      # 修改为实际的域名（通常使用 admin. 前缀）
      # 证书配置在 dynamic/certs.yml 中统一管理
      - traefik.http.routers.admin-example.rule=Host(`admin.example.com`)
      - traefik.http.routers.admin-example.entrypoints=websecure
      - traefik.http.routers.admin-example.tls=true

    # 网络配置
    networks:
      - nfx-edge

    # 服务依赖
    depends_on:
      - reverse-proxy

  # === 🦊 Gitea (Web + SSH, behind Traefik) ===
  gitea:
    image: gitea/gitea:latest
    container_name: NFX-Edge-Gitea
    restart: always
    # SSH 不能用 22（宿主机一般被 NAS 占用），所以映射到 2224
    ports:
      - "${GITEA_SSH_BIND_HOST:-0.0.0.0}:${GITEA_SSH_PORT:-2224}:22"

    # Web 不直接暴露宿主机端口，交给 Traefik 通过同网络访问
    expose:
      - "3000"

    environment:
      - USER_UID=1000
      - USER_GID=1000
      - GITEA__database__DB_TYPE=sqlite3
      - GITEA__database__PATH=/data/gitea/gitea.db
      - GITEA__server__DOMAIN=${GITEA_DOMAIN}
      - GITEA__server__ROOT_URL=https://${GITEA_DOMAIN}
      - GITEA__server__SSH_DOMAIN=${GITEA_DOMAIN}
      - GITEA__server__SSH_PORT=${GITEA_SSH_PORT}
      - GITEA__server__SSH_LISTEN_PORT=22
      - GITEA__server__HTTP_PORT=3000
      - GITEA__server__PROTOCOL=http
      - GITEA__server__ENABLE_PPROF=false
      - GITEA__log__ROOT_PATH=/data/gitea/log
      - GITEA__repository__ROOT=/data/git/repositories
      - GITEA__security__INSTALL_LOCK=true
      - TZ=${GITEA_TZ:-America/Vancouver}

    volumes:
      - ${GITEA_DATA_VOLUME}:/data
      - ${GITEA_CONFIG_VOLUME}:/etc/gitea

    labels:
      - traefik.enable=true
      - traefik.project=nfx-edge

      # -------- Gitea Web --------
      - traefik.http.routers.gitea.rule=Host(`${GITEA_DOMAIN}`)
      - traefik.http.routers.gitea.entrypoints=websecure
      - traefik.http.routers.gitea.tls=true
      - traefik.http.services.gitea.loadbalancer.server.port=3000

      # （推荐）安全头：HSTS 等（你已全站走 https 的情况下很合适）
      - traefik.http.middlewares.gitea-sec.headers.stsSeconds=31536000
      - traefik.http.middlewares.gitea-sec.headers.stsIncludeSubdomains=true
      - traefik.http.middlewares.gitea-sec.headers.stsPreload=true
      - traefik.http.routers.gitea.middlewares=gitea-sec@docker

    networks:
      - nfx-edge


# 网络定义
networks:
  nfx-edge:
    name: nfx-edge
    driver: bridge
