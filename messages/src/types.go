package messages

// Command / notification copy lives in messages/src (comment blocks).
// Generate: go run ./messages/cmd/gen_langs/ ./messages
// Served as GET /edge/{tls,dns,file,analysis}/messages/:lang
